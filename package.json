{
  "name": "isabelle-pide-stock",
  "displayName": "Isabelle/PIDE (stock VS Code)",
  "description": "Isabelle proof assistant support for unmodified VS Code, driving `isabelle vscode_server` over LSP.",
  "version": "0.0.1",
  "publisher": "spike",
  "license": "BSD-3-Clause",
  "categories": [
    "Programming Languages"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:isabelle"
  ],
  "contributes": {
    "languages": [
      {
        "id": "isabelle",
        "aliases": [
          "Isabelle",
          "isabelle"
        ],
        "extensions": [
          ".thy"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "commands": [
      {
        "command": "isabelle.restartServer",
        "title": "Isabelle: Start / Restart Language Server"
      },
      {
        "command": "isabelle.normalizeDocument",
        "title": "Isabelle: Normalize Symbols to ASCII"
      },
      {
        "command": "isabelle.toggleSymbolRendering",
        "title": "Isabelle: Toggle Unicode Symbol Rendering"
      },
      {
        "command": "isabelle.stateUpdate",
        "title": "Isabelle: Update Proof State"
      },
      {
        "command": "isabelle.stateToggleAutoUpdate",
        "title": "Isabelle: Toggle Proof State Auto-Update"
      },
      {
        "command": "isabelle.sledgehammer",
        "title": "Isabelle: Run Sledgehammer"
      },
      {
        "command": "isabelle.include-word",
        "title": "Include word"
      },
      {
        "command": "isabelle.include-word-permanently",
        "title": "Include word permanently"
      },
      {
        "command": "isabelle.exclude-word",
        "title": "Exclude word"
      },
      {
        "command": "isabelle.exclude-word-permanently",
        "title": "Exclude word permanently"
      },
      {
        "command": "isabelle.reset-words",
        "title": "Reset non-permanent words"
      },
      {
        "command": "isabelle.preview",
        "title": "Isabelle: Open Preview"
      },
      {
        "command": "isabelle.previewSplit",
        "title": "Isabelle: Open Preview (Split Editor)"
      },
      {
        "command": "isabelle.documentation",
        "title": "Isabelle: Show Documentation"
      },
      {
        "command": "isabelle.findTheorems",
        "title": "Isabelle: Find Theorems / Constants"
      },
      {
        "command": "isabelle.theoriesRefresh",
        "title": "Isabelle: Refresh Theories",
        "icon": "$(refresh)"
      },
      {
        "command": "isabelle.selectSession",
        "title": "Isabelle: Select Session Image"
      },
      {
        "command": "isabelle.toggleContinuousChecking",
        "title": "Isabelle: Toggle Continuous Checking"
      },
      {
        "command": "isabelle.gotoCommand",
        "title": "Isabelle: Go to Command"
      },
      {
        "command": "isabelle.simplifierTrace",
        "title": "Isabelle: Show Simplifier Trace"
      }
    ],
    "keybindings": [
      {
        "command": "isabelle.cursorLeft",
        "key": "left",
        "when": "editorTextFocus && editorLangId == isabelle && config.isabelle.renderSymbols"
      },
      {
        "command": "isabelle.cursorRight",
        "key": "right",
        "when": "editorTextFocus && editorLangId == isabelle && config.isabelle.renderSymbols"
      },
      {
        "command": "isabelle.cursorLeftSelect",
        "key": "shift+left",
        "when": "editorTextFocus && editorLangId == isabelle && config.isabelle.renderSymbols"
      },
      {
        "command": "isabelle.cursorRightSelect",
        "key": "shift+right",
        "when": "editorTextFocus && editorLangId == isabelle && config.isabelle.renderSymbols"
      },
      {
        "command": "isabelle.deleteLeft",
        "key": "backspace",
        "when": "editorTextFocus && !editorReadonly && editorLangId == isabelle && config.isabelle.renderSymbols"
      },
      {
        "command": "isabelle.deleteRight",
        "key": "delete",
        "when": "editorTextFocus && !editorReadonly && editorLangId == isabelle && config.isabelle.renderSymbols"
      }
    ],
    "configurationDefaults": {
      "[isabelle]": {
        "editor.wordSeparators": "`~!@#$%^&*()-=+[{]}|;:'\",./?"
      }
    },
    "configuration": {
      "title": "Isabelle",
      "properties": {
        "isabelle.autoStart": {
          "type": "boolean",
          "default": true,
          "description": "Start the language server when the extension activates. Turn this off to open .thy files without a prover -- symbol rendering, the outline and symbol input all work without one, and starting a session loads a heap image, which takes a while. Use \"Isabelle: Start / Restart Language Server\" when you want it."
        },
        "isabelle.continuousChecking": {
          "type": "boolean",
          "default": false,
          "description": "Check the whole visible theory rather than a window around the caret (vscode_caret_perspective=0). Takes effect on server restart."
        },
        "isabelle.expandAbbreviations": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Expand `\\forall` into `\\<forall>` as you type."
        },
        "isabelle.home": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the Isabelle distribution (the directory containing `bin/isabelle`). Empty: use `$ISABELLE_HOME`, then well-known install locations."
        },
        "isabelle.logic": {
          "type": "string",
          "default": "HOL",
          "description": "Session image the language server boots with (isabelle vscode_server -l/-R). Imports already in this image load from a heap and are never re-checked; every other theory is elaborated from source on each start. The default \"HOL\" caches no project theory. Use \"Isabelle: Select Session Image\" to pick one from the ROOT files in the workspace."
        },
        "isabelle.logicRequirements": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Start from an image built of `#isabelle.logic#`'s **requirements** (`-R`) rather than the session itself (`-l`). Use this when the theories you are editing belong to that session: everything they import is then loaded from a prebuilt heap and is never re-checked, while your own files stay live.",
          "description": "Boot with the chosen session's *requirements* (-R) rather than the session itself (-l). This is what you want while editing that session: everything it imports comes from a heap, while its own theories stay live and editable. With -l the session's own theories are in the image too, so they are not checked at all."
        },
        "isabelle.markupColors": {
          "type": "string",
          "enum": [
            "theme",
            "isabelle"
          ],
          "default": "theme",
          "enumDescriptions": [
            "Colour checked text from the active colour theme, by serving PIDE markup as semantic tokens. Installing a theme then recolours the whole file.",
            "Colour checked text from Isabelle's own text_color palette, as Isabelle/jEdit and the official Isabelle/VSCode do. The theme will not affect checked text."
          ],
          "markdownDescription": "Who colours text that PIDE has checked. Processing status, message underlines and overview marks are unaffected either way. Note that `theme` relies on semantic highlighting, which `#editor.semanticHighlighting.enabled#` leaves to the theme by default; a theme that opts out falls back to the TextMate grammar."
        },
        "isabelle.normalizeOnSave": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Before saving, rewrite any literal Unicode symbols back to `\\<name>` escapes. `isabelle build` rejects literal Unicode, so this keeps files loadable."
        },
        "isabelle.pideMarkup": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Apply PIDE markup (syntax colouring and processing status) as editor decorations."
        },
        "isabelle.pideViewportScope": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Apply PIDE markup only to the visible part of the document (plus `#isabelle.renderMarginLines#`). The server sends markup for the whole file - over 51000 ranges on a 9000-line theory - and applying all of it costs roughly 40ms per keystroke. Disable only to compare against that behaviour."
        },
        "isabelle.queryPanel": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Show the Query panel (find_theorems / find_consts). Requires an Isabelle whose language server exposes the `PIDE/query_*` messages; released versions do not. See the `vscode-query-panel` branch of mirror-isabelle."
        },
        "isabelle.renderMarginLines": {
          "type": "number",
          "default": 100,
          "markdownDescription": "How many lines above and below the viewport to decorate. Cost is linear in decorated ranges; keep the resulting count well under ~1500."
        },
        "isabelle.renderSymbols": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Render `\\<forall>`-style symbol escapes as Unicode glyphs using editor decorations. The file on disk stays ASCII."
        },
        "isabelle.revealSymbolAtCursor": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show the raw `\\<name>` escape instead of the glyph for the symbol the cursor is inside, so it can be edited."
        },
        "isabelle.serverArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional raw arguments appended to isabelle vscode_server."
        },
        "isabelle.serverOptions": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "vscode_pide_extensions=true",
            "vscode_html_output=true",
            "vscode_unicode_symbols_output=true"
          ],
          "markdownDescription": "Isabelle system options passed as `-o NAME=VALUE`.\n\n`vscode_pide_extensions` enables PIDE markup decorations and the Output/State panels. `vscode_html_output` makes those panels deliver HTML, and also avoids an Isabelle2025-2 bug where the non-HTML branch fails to serialise decorations. `vscode_unicode_symbols_output` renders glyphs in panels and hovers.\n\nDo **not** enable `vscode_unicode_symbols_edits`: those edits are inserted into the buffer, which must stay ASCII."
        },
        "isabelle.sessionDirs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra session directories (isabelle vscode_server -d)."
        },
        "isabelle.simplifierTrace": {
          "type": "boolean",
          "default": false,
          "description": "Show the Simplifier Trace panel. The PIDE/simplifier_trace_* messages exist only on the vscode-simplifier-trace branch of mirror-isabelle, so against a stock distribution this view would stay empty. Enable tracing in a theory with declare [[simp_trace_new mode=full]]."
        },
        "isabelle.spellChecker": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Use Isabelle's own spell checker, which checks prose in comments and `text ‹…›` blocks by PIDE markup category (so antiquotations inside prose are excluded). Turn this off to avoid double underlining if you prefer a general spell-checking extension such as Code Spell Checker; the bundled TextMate grammar gives those extensions `comment.block.isabelle` and `string.quoted.*` scopes to target."
        },
        "isabelle.textColorOverrides": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "markdownDescription": "Override individual PIDE markup colours, keyed `<name>_light` / `<name>_dark` (e.g. `keyword1_dark`). Defaults come from Isabelle's own palette."
        },
        "isabelle.theoriesPanel": {
          "type": "boolean",
          "default": false,
          "description": "Show the Theories and Timing views. Requires the vscode-theories-panel branch of mirror-isabelle; a released Isabelle does not answer PIDE/theories_request."
        },
        "isabelle.timingThreshold": {
          "type": "number",
          "default": 0.1,
          "minimum": 0,
          "description": "Timing view: only commands at or above this many seconds are reported."
        },
        "isabelle.verbose": {
          "type": "boolean",
          "default": false,
          "description": "Verbose language-server logging (-v)."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "isabelle",
          "title": "Isabelle",
          "icon": "media/isabelle.svg"
        }
      ],
      "panel": [
        {
          "id": "isabelle-panel",
          "title": "Isabelle",
          "icon": "media/isabelle.svg"
        }
      ]
    },
    "views": {
      "isabelle": [
        {
          "id": "isabelle-state",
          "name": "State",
          "type": "webview"
        },
        {
          "id": "isabelle-theories",
          "name": "Theories",
          "when": "config.isabelle.theoriesPanel"
        },
        {
          "id": "isabelle-timing",
          "name": "Timing",
          "when": "config.isabelle.theoriesPanel",
          "visibility": "collapsed"
        },
        {
          "id": "isabelle-sledgehammer",
          "name": "Sledgehammer",
          "type": "webview"
        },
        {
          "id": "isabelle-symbols",
          "name": "Symbols",
          "type": "webview"
        },
        {
          "id": "isabelle-documentation",
          "name": "Documentation",
          "type": "webview"
        },
        {
          "id": "isabelle-query",
          "name": "Query",
          "type": "webview",
          "when": "config.isabelle.queryPanel"
        },
        {
          "id": "isabelle-simplifier-trace",
          "name": "Simplifier Trace",
          "type": "webview",
          "when": "config.isabelle.simplifierTrace"
        }
      ],
      "isabelle-panel": [
        {
          "id": "isabelle-output",
          "name": "Output",
          "type": "webview"
        }
      ]
    },
    "grammars": [
      {
        "language": "isabelle",
        "scopeName": "source.isabelle",
        "path": "./syntaxes/isabelle-grammar.json"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "isabelle.theoriesRefresh",
          "when": "view == isabelle-theories",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "isabelle.gotoCommand",
          "when": "false"
        }
      ]
    },
    "semanticTokenTypes": [
      {
        "id": "isabelleProofKeyword",
        "superType": "keyword",
        "description": "Isabelle proof command (by, apply, done)"
      },
      {
        "id": "isabelleInnerKeyword",
        "superType": "type",
        "description": "Isabelle inner-syntax keyword"
      },
      {
        "id": "isabelleQuasiKeyword",
        "superType": "keyword",
        "description": "Isabelle quasi-keyword"
      },
      {
        "id": "isabelleImproper",
        "superType": "keyword",
        "description": "Isabelle improper command"
      },
      {
        "id": "isabelleSkolem",
        "superType": "variable",
        "description": "Isabelle Skolem variable"
      },
      {
        "id": "isabelleSchematic",
        "superType": "variable",
        "description": "Isabelle schematic variable"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "isabelle",
        "scopes": {
          "isabelleProofKeyword": [
            "keyword.control.isabelle",
            "keyword.control"
          ],
          "isabelleInnerKeyword": [
            "support.type.isabelle",
            "support.type",
            "entity.name.type"
          ],
          "isabelleQuasiKeyword": [
            "keyword.other.isabelle",
            "keyword.other"
          ],
          "isabelleImproper": [
            "invalid.deprecated",
            "keyword.other"
          ],
          "isabelleSkolem": [
            "variable.other.isabelle",
            "variable.other"
          ],
          "isabelleSchematic": [
            "variable.other.constant",
            "variable.other"
          ]
        }
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "dev": "node ./test/dev.js",
    "test:unit": "node ./test/suite25.js",
    "test": "node ./test/runTest.js",
    "package": "vsce package --out isabelle-pide-stock.vsix"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.9",
    "@vscode/vsce": "^3.2.0",
    "typescript": "^5.4.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/real-yfprojects/vscode-isabelle.git"
  }
}
